# Plan 004 - Task Storage

## Storing Tasks Client-Side

Currently, when tasks are added, they are just in browser memory and do not persist a page reload or closing the browser. All tasks added by the user should be saved in their browser's local storage.

In addition to the storage of tasks, the context note should also be stored.